out
lib
cache
node_modules
report
reports
# forge script broadcast artifacts (gitignored; written by the anvil integration test)
broadcast
# explainer webapp build output
dist
_corpus
crytic-export
snapshots
audits
# vendored agent skills (managed by npx skills; formatting would break hash verification)
.agents
